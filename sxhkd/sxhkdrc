## Keybindings config for Archcraft's BSPWM

##---------- Keybindings for bspwm ----------##

# Terminal
alt + Return
	kitty

# Terminal (floating)
# alt + shift + Return
# 	bspterm --float

##---------- Rofi Launcher & Menus ----------##

# Rofi App Launcher
alt + d
	rofi -no-config -no-lazy-grab -show drun -modi drun -theme ~/.config/rofi/aesthetic-night.rasi

##---------- Applications ----------##

# Launch Apps
alt + {e,c,g,m,o}
	{thunar,code,microsoft-edge-stable,kitty -e ncmpcpp,obsidian}

# Color Picker
alt + p
	sh ~/.config/bspwm/scripts/xcolor-pick.sh

# Lockscreen
super + l
  sh ~/.config/bspwm/scripts/lock.sh

# Restart polybar
alt + shift +p
  sh ~/.config/polybar/launch.sh

# Change wallpaper
alt + shift + w
  feh --no-fehbg --bg-fill --randomize ~/Data/Nutstore/Pictures/Wallpaper/*

##---------- System Keys ----------##

# Take a screenshot
Print
	flameshot full

# Take screenshot of area
alt + shift + f
	flameshot gui

# Brighness control
XF86MonBrightness{Up,Down}
	sh ~/.config/bspwm/scripts/brightness {--inc,--dec}

# Volume control
XF86Audio{RaiseVolume,LowerVolume}
	sh ~/.config/bspwm/scripts/volume {--inc,--dec}

XF86AudioMute
	sh ~/.config/bspwm/scripts/volume --toggle

# XF86AudioMicMute
# 	volume --toggle-mic

# Music control
XF86Audio{Next,Prev,Play,Stop}
  mpc {next,prev,toggle,stop}

##---------- Bspwm ----------##

# Close App
# alt + {_,shift + }q
# 	bspc node -{k,c}
alt + shift + q
	bspc node -c

# Reload Keybindings
alt + r
	pkill -USR1 -x sxhkd

# kill window
# ctrl + alt + Escape
#     xkill

# Quit/Restart bspwm
alt + shift + {x,r}
	bspc {quit,wm -r}

# Split horizontal, vertical or cancel
# alt + {h,v,q}
	# bspc node -p {east,south,cancel}

# Preselect the ratio
# alt + ctrl + {1-9}
# 	bspc node -o 0.{1-9}

# Fullscreen or Monocle
alt + f
  bspc node -t "~"fullscreen

# Toggle beetwen floating & tiled
alt + space
  bspc node -t "~"{floating,tiled}

# Pseudo Tiled & tiled mode
# alt + shift + space
  # bspc node -t "~"{pseudo_tiled,tiled}

# Set the node flags
# alt + shift + {m,x,y,z}
	# bspc node -g {marked,locked,sticky,private}

# Send the window to another edge of the screen
alt + {_,shift + }{Left,Down,Up,Right}
	bspc node -{f,s} {west,south,north,east}
alt + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# Change focus to next window, including floating window
alt + {_,shift + }Tab
	bspc node -f {next.local,prev.local}

# Switch workspace
# ctrl + alt + {Left,Right}
	# bspc desktop -f {prev.local,next.local}

# Switch to last opened workspace
# alt + {Tab,grave}
	# bspc {node,desktop} -f last

# Send focused window to another workspace
alt + {_,shift + }{1-9}
	bspc {desktop -f,node -d} '^{1-9}'

# Expanding windows
alt + control + {Left,Right,Up,Down}
	bspc node -z {left -20 0,right 20 0,top 0 -20,bottom 0 20}
alt + control + {h,l,k,j}
	bspc node -z {left -20 0,right 20 0,top 0 -20,bottom 0 20}

## Shrinking windows
# super + alt + {Left,Right,Up,Down}
# 	bspc node -z {left 20 0,right -20 0,top 0 20,bottom 0 -20}

## Move floating windows
# alt + shift + {Left,Down,Up,Right}
# 	bspc node -v {-20 0,0 20,0 -20,20 0}

# Hide/Unhide Window
# alt + shift + n
# 	bspwinmask

##---------- EOF ----------##
